{
  "name": "ecv",
  "private": true,
  "version": "0.0.0",
  "author": "Jin-K",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "tsc && vite build",
    "deploy": "git subtree push --prefix dist origin gh-pages",
    "publish": "npm run build && git add . && git add dist -f && git commit -m \"build for gh-pages\" && npm run deploy",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.49.11",
    "typescript": "^4.5.4",
    "vite": "^2.9.0"
  },
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "sfmediastream": "^1.3.1",
    "socket.io-client": "^4.4.1"
  }
}
